质感/风格参考图目录（仅动漫风格生效）
=========================================
把你觉得有质感的漫画/动漫图片放在此目录下（.png / .jpg / .jpeg / .webp），最多取前 3 张。

选「动漫风格」生成剧情图时，这些图会作为「仅参考线条、网点、阴影质感」传给生图模型，
模型会尽量模仿这些图的画风，但不会照抄图中的人物或构图。角色仍以主角/配角参考图为准。

可通过 .env 的 STYLE_REFERENCE_DIR 修改目录路径（默认即本目录）。
