# Git 改进清单（ZYC 分支相对 main）

按时间从旧到新列出 ZYC 分支上每个提交在做什么，便于回溯与排查。

---

## 一、早期：合并与基础

| 提交 | 简要说明 |
|------|----------|
| **dc856a4** C10 | 早期功能/配置提交。 |
| **76e23c8** Merge someone2/main | 合并远程 someone2/main 到当前分支。 |
| **b446f41** bug修复基本完成版 | 一轮 bug 修复收尾。 |
| **604bef0** C11:预生成图片功能 | 增加**预生成图片**能力：在预生成剧情时一并生成/缓存场景图，减少用户选择后等待。 |
| **7781e75** 无所畏惧 | 小改动/重命名或信心标记。 |
| **f93b269** Merge upstream1/推倒重来 | 合并「推倒重来」分支，可能含较大重构或方向调整。 |

---

## 二、功能迭代（C12–C16）

| 提交 | 简要说明 |
|------|----------|
| **dc26d4b** C12 | 功能或逻辑小步迭代。 |
| **b516932** C13 | 同上。 |
| **150e6cc** C14 | 同上。 |
| **6c365e9** C15 | 同上。 |
| **741d46b** C16:显示数字！ | **UI/展示**：在界面或剧情中**显示数字**（如选项序号、进度等），提升可读性。 |
| **d322a8b** C17:更快 | **性能/体验**：某处逻辑或请求**加速**（可能含超时、并发或缓存调整）。 |

---

## 三、主角与配角形象/依赖（C18–C21）

| 提交 | 简要说明 |
|------|----------|
| **f3ae694** C18:主角形象依赖生成。。。 | **主角图**：主角立绘改为**依赖生成**（如先有世界观/设定再生成形象），保证与设定一致。 |
| **5290b60** C19:真·主角依赖生成！！！ | 继续强化**主角依赖生成**逻辑，确保生成顺序与数据完整。 |
| **aea8cfd** C20:配角依赖生成！ | **配角**：配角身份/档案改为**依赖生成**（依赖剧情或档案数据），避免凭空生成。 |
| **978ecbc** C21:配角动态生成 | **配角**：实现**配角动态生成**——随剧情推进识别、建档、生成参考图等。 |
| **2066ff7** C22 | 配合 C21 的后续调整或修复。 |

---

## 四、代码拆分与文档（C23 + 说明文档 + game_server 拆分）

| 提交 | 简要说明 |
|------|----------|
| **213e8b9** C23:拆代码！ | **架构**：把大块逻辑从单文件**拆到** `src/` 下多模块（如 llm、image、story、characters 等），便于维护。 |
| **7d2bb60** 说明文档1 | 新增**说明文档**（如架构、流程、配置等）第一部分。 |
| **50bc6be** 说明文档2 | 说明文档第二部分或补充。 |
| **e7ceac4** game_server.py拆分 | **架构**：将 `game_server.py` 中配置、缓存、预生成、工具、图片缓存等**拆到** `server/` 包（config、cache、utils、pregeneration、image_cache），主文件只保留路由与启动。 |
| **2ba3be0** AI全部应用为顶模，并添加了AI使用说明文档 | **AI 配置与文档**：统一使用**顶模**（如 Claude Opus、Gemini 等），并新增 **AI 使用说明文档**（模型位置、用途、环境变量等，即 `docs/AI_USAGE.md` 等）。 |
| **1bc217a** 基调prompt优化 | **Prompt**：对故事**基调（tone）** 相关 prompt 做优化，使生成风格更稳定。 |
| **b3e5d0d** 生成内容优化 | 对 LLM/图像**生成内容**的 prompt 或后处理做优化。 |

---

## 五、融合与配角大改（C24–C27）

| 提交 | 简要说明 |
|------|----------|
| **f4cfacf** C24:llm群体智能(垃圾版) | 尝试**LLM 群体智能**（多模型/多角色协作），自标为实验/垃圾版。 |
| **8db59c0** 解决合并冲突：融合AI逻辑优化分支 | 合并 **AI 逻辑优化** 分支并解决冲突，融合顶模、prompt、文档等改动。 |
| **2240204** C25:融合形态！ | 与 C24、合并结果一起形成**融合形态**：顶模 + 文档 + 拆分 + 优化。 |
| **dcc71d1** C26:万恶的配角生成。。。 | **配角系统大改**：配角识别、建档时机、参考图生成等逻辑重做，解决「万恶的配角」相关问题。 |
| **f147de5** C27 | 配合 C26 的后续修正或小功能。 |

---

## 六、剧情/选项/预生成/图尺寸（Z1–Z4）

| 提交 | 简要说明 |
|------|----------|
| **30633fd** Z1：修改了剧情生成图的尺寸、默认剧情补图、超时拆分、剧情解析加固（格式约束、代码块剥离、兼容无【】、失败时打印调试）、配角生成大改（建档改展示后触发、剧情输出出场/提及配角、预配角、视觉裁单人图、名解析加固、预生成第二层生图） | **综合大改**：① 剧情生成图**尺寸**调整；② **默认剧情补图**逻辑；③ **超时拆分**（大请求拆成多段）；④ **剧情解析加固**：格式约束、代码块剥离、兼容无【】、失败时打印调试；⑤ **配角**：建档改为**展示后触发**、剧情输出**出场/提及配角**、**预配角**、**视觉裁单人图**、**名解析加固**、**预生成第二层生图**。 |
| **5ffa283** Z2：合并1 | 合并一批改动（可能含 new-version 或本地分支）。 |
| **eb6f47c** Z3：合并2 | 继续合并。 |
| **1775624** Merge new-version into ZYC | 将 **new-version** 分支合并进 ZYC。 |
| **bdda0a8** Z4:合并主线剧情选项生成，文本模式兜底 | **选项与兜底**：**合并主线剧情选项生成**逻辑（与主线一致），并增加**文本模式兜底**（图失败或关闭时仍可玩纯文本）。 |

---

## 七、JSON 剧情提示词与主角/配角修正（C28–C32）

| 提交 | 简要说明 |
|------|----------|
| **ec899c2** C28:json剧情提示词 | **提示词**：剧情/图像相关 prompt 改为或支持 **JSON 结构化**（便于约束输出格式、多语言、多字段）。 |
| **c2cb246** json | 继续 **JSON** 相关改动（如 `prompt_optimize.py` 中提示词格式、解析）。 |
| **e3655a6** C29:主角&配角修正 | **主角与配角**：修正主角/配角生成或展示的 bug（可能含 `vision_ref_crop.py`、`api_providers.py`、`prompt_optimize.py` 等）。 |
| **cf523ad** C30 | 小步迭代（含前端 index.html、配角/主角数据、`prompt_optimize.py` 等）。 |
| **d3dd11f** C31:预生成修复 | **预生成**：修复预生成流程的 bug（`game_server.py` 大幅精简因已拆分到 `server/`、`server/pregeneration.py`、config、api_providers、prompt_optimize、storage 等）；并整理/删除部分 initial 测试数据。 |
| **29b0fe1** C32 | 继续预生成/提示词/测试脚本等（如 `prompt_optimize.py`、`test_gemini_image.py`、配角与主角资源）。 |

---

## 八、收尾与冲突解决

| 提交 | 简要说明 |
|------|----------|
| **37b125a** Z5:总结1 | **总结**：对当前周期改动的总结或收尾（如更新 `pending_roles.json` 等）。 |
| **11e9915** 解决pending_roles.json文件modify/delete冲突：按远程版本删除文件 | **冲突解决**：解决 `pending_roles.json` 的 **modify/delete 冲突**，采用**按远程版本删除文件**的策略，保证与远程一致。 |

---

## 九、按主题归类速查

| 主题 | 相关提交 |
|------|----------|
| **架构/拆分** | C23 拆代码，game_server 拆分，说明文档1/2 |
| **AI 模型与文档** | 顶模 + AI 使用说明文档，基调 prompt 优化，生成内容优化 |
| **主角/配角** | C18–C22 依赖与动态生成，C26 配角大改，Z1 配角建档/预配角/裁图，C29 主角&配角修正 |
| **预生成** | C11 预生成图片，Z1 预生成第二层生图，C31 预生成修复 |
| **剧情/选项** | Z1 剧情解析与尺寸，Z4 合并主线选项+文本兜底，C28/json 剧情提示词 |
| **冲突与收尾** | 融合 AI 逻辑优化分支，Z5 总结，pending_roles 冲突解决 |

以上即 ZYC 分支上各次改进的清单与简要说明；具体代码以仓库为准。
